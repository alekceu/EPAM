Question
1. Не сработала установка переменой из скрипта export var=111
2. не работает обращение к масиву ${$i}
for (( i=1; i<=$#; i++));
do
  echo "Arg$i:${@:$i:1}" - работает только так !!! Почему?
done
3. > /dev/null 2>&1 - подавляет все перенаправления
Есть ли вариант покороче и что означает &1 в конце

4. Unicat ? Multicast and Broadcast??? В чем разница?

5. Rebase - поиггратся  с вариантами


Answer
2. var=${1:-5} - Это значит либо будет использоватся первый параметр и его значение
или значение по умолчанию 5.
